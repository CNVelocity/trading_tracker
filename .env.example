# Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/trading_tracker?sslmode=require

# Single-user Bearer token for API authentication
API_SECRET_TOKEN=change_me_to_a_long_random_string

# Optional: secret for signing JWT tokens (falls back to API_SECRET_TOKEN)
TOKEN_SECRET=
